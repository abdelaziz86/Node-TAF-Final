<%- include("partials/header") %>

    <h2 class="text-center">➕ Ajouter un Nouveau Livre</h2>

    <form action="/add" method="POST" class="p-4 bg-light shadow-sm rounded">
        <div class="mb-3">
            <label class="form-label">Titre</label>
            <input type="text" name="title" class="form-control" required minlength="3">
        </div>
        <div class="mb-3">
            <label class="form-label">Auteur</label>
            <input type="text" name="author" class="form-control" required minlength="3">
        </div>
        <div class="mb-3">
            <label class="form-label">Année de publication</label>
            <input type="number" name="year" class="form-control" required max="<%= new Date().getFullYear() %>">
        </div>
        <div class="mb-3">
            <label class="form-label">Résumé</label>
            <textarea name="summary" class="form-control" maxlength="500"></textarea>
        </div>
        <button type="submit" class="btn btn-primary w-100">Ajouter 📖</button>
    </form>

    <%- include("partials/footer") %>